<%- include('head') %>
<body>
  <%- include('navbar') %>
  <div class="container">
    <h1>This is the dashboard page</h1>
    <div class="row">
      <% for (let i = 0; i < meals.length; i++) { %>
        <div class="col s3">
          <%= `${meals[i].label} Calories: ${meals[i].calories} Fat: ${fat} ` %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
